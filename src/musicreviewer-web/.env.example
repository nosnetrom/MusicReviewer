# Base URL of the MusicReviewer API for deployed builds (e.g. https://app-musicreviewer-dev.azurewebsites.net).
# Leave empty for local development; Vite proxies /api to http://localhost:5080.
VITE_API_BASE_URL=
